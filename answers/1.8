docker run -dp 5000:5000 -e CURRENT_ENVIRONMENT=development lisacharuel/container

#Before the image start, it says "Unable to find image 'lisacharuel/container:latest' locally"
#We pull the image from DockerHub then run it.

